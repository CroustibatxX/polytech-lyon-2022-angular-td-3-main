import { BagageVolDirective } from './bagage-vol.directive';

describe('BagageVolDirective', () => {
  it('should create an instance', () => {
    const directive = new BagageVolDirective();
    expect(directive).toBeTruthy();
  });
});
